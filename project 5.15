arrow_base_height = int(input('Enter arrow base height:\n'))

arrow_base_width = int(input('Enter arrow base width:\n'))

arrow_head_width = int(input('Enter arrow head width:\n'))


# Draw arrow base (height = 3, width = 2)
#This function from assignment only executes loop for arrow if the head width > base width
while arrow_head_width <= arrow_base_width:
    arrow_head_width = int(input('Enter arrow head width:\n'))
else:    
#this part makes the rectangle base    
    print('')
    arrow_base_width = (arrow_base_width)
    arrow_base_height = (arrow_base_height)
    for p in range(arrow_base_height):
            
        for i in range(arrow_base_width):
        
            print('*', end='')
        print('')
    
    # Draw arrow head (width = 4)
      
 #this part makes the arrow head part       
    
    for star in range(arrow_head_width):
        for num in range(arrow_head_width):
        if (num+1)-star >= (arrow_head_width - (arrow_head_width-1)):
            print('*', end='')
        print()

    
